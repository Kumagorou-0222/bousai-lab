# タスクリスト：防災Lab 収益化改善

- `[ ]` 環境変数 `.env.local` の新規作成・設定
- `[ ]` `lib/products.ts` 内のアフィリエイトURL特定商品個別化 ＆ ローリングストック対応
- `[ ]` `components/ComparisonTable.tsx` へのアフィリエイトボタン描画追加
- `[ ]` `app/manga/[slug]/page.tsx` 内の `ComparisonTable` にProps（amazonUrl, rakutenUrl）を渡す
- `[ ]` `app/articles/[slug]/page.tsx` 内の `ComparisonTable` にProps（amazonUrl, rakutenUrl）を渡す
- `[ ]` `app/manga/[slug]/page.tsx` への `AdSense` 配置（4コマ下、CTA手前）
- `[ ]` `app/manga/page.tsx` への `AdSense` 配置（セクション中間）
- `[ ]` `npm run build` によるビルド検証
